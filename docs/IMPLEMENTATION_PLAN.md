# FantaScan AI - SEO/AEO/GEO 최적화 구현 계획서

> **목표**: 브랜드의 SEO/Answer Engine Optimization/Generative Engine Optimization 효과를 추적하고, 시간에 따른 개선을 측정하며, 풍부한 리포트를 생성하는 도구

---

## 📊 현재 상태 분석

### 기존 구현 완료 항목
| 기능 | 상태 | 설명 |
|------|------|------|
| 멀티 AI 프로바이더 쿼리 | ✅ | OpenAI, Gemini, Claude, Grok, Perplexity |
| 브랜드 멘션 감지 | ✅ | 위치, 감성, 컨텍스트 분석 |
| Share of Voice 계산 | ✅ | 경쟁사 대비 점유율 |
| 트렌드 분석 | ✅ | 7일/30일/90일 추이 |
| 인용 추출 | ✅ | Perplexity 등에서 출처 추출 |
| 환각 감지 | ✅ | 브랜드 관련 허위정보 탐지 |
| 알림 시스템 | ✅ | 가시성 변화 알림 |
| 리포트 생성 | ⚠️ | 기본 텍스트 리포트만 |

### 미구현/개선 필요 항목
| 기능 | 우선순위 | 설명 |
|------|----------|------|
| **파생 쿼리 자동 생성** | 🔴 Critical | 하나의 쿼리에서 유사/관련 쿼리 자동 생성 |
| **다중 시나리오 테스트** | 🔴 Critical | 다양한 시나리오로 풍부한 데이터 수집 |
| **키워드 노출량 정량화** | 🔴 Critical | 키워드별 노출 레벨 측정 및 추적 |
| **시계열 대시보드** | 🟡 High | 노력 대비 개선 추이 시각화 |
| **종합 리포트** | 🟡 High | PDF/이메일 발송 가능한 상세 리포트 |
| **벤치마크 비교** | 🟢 Medium | 산업 평균 대비 위치 |

---

## 🎯 핵심 구현 계획

### Phase 1: 파생 쿼리 자동 생성 시스템 (1주)

#### 1.1 Query Expansion Service
```
사용자 입력: "SaaS 소프트웨어 추천해줘"
↓
자동 생성 파생 쿼리:
├── 의도 변형: "SaaS 소프트웨어 비교해줘"
├── 구체화: "스타트업용 SaaS 소프트웨어 추천"
├── 가격 관점: "가성비 좋은 SaaS 도구"
├── 대안 탐색: "{브랜드명} 대안 추천"
├── 비교 쿼리: "{브랜드명} vs {경쟁사} 비교"
├── 후기 쿼리: "{브랜드명} 실제 사용 후기"
├── 순위 쿼리: "2024년 SaaS 소프트웨어 순위"
└── 기능별: "협업 기능 좋은 SaaS 도구"
```

#### 구현 파일
```typescript
// src/services/query-expansion/index.ts
interface QueryExpansionResult {
  originalQuery: string;
  derivedQueries: DerivedQuery[];
  expansionStrategy: ExpansionStrategy[];
}

interface DerivedQuery {
  query: string;
  type: 'intent_variation' | 'specificity' | 'price_focus' | 'alternative' | 
        'comparison' | 'review' | 'ranking' | 'feature_specific';
  relevanceScore: number;
  expectedInsight: string;
}
```

#### 1.2 LLM 기반 쿼리 확장
```typescript
// AI를 활용한 동적 쿼리 생성
export const QUERY_EXPANSION_PROMPT = (input: {
  originalQuery: string;
  brandName: string;
  industry: string;
  keywords: string[];
  competitors: string[];
}) => `당신은 SEO/AEO 전문가입니다.

원본 쿼리: "${input.originalQuery}"
브랜드: ${input.brandName}
산업: ${input.industry}
키워드: ${input.keywords.join(', ')}
경쟁사: ${input.competitors.join(', ')}

이 쿼리를 기반으로 브랜드 가시성을 테스트할 수 있는 8-12개의 파생 쿼리를 생성하세요.

다양한 관점 포함:
1. 직접 추천 요청 (추천해줘, 알려줘)
2. 비교 요청 (vs, 비교, 차이점)
3. 순위/랭킹 (1위, 베스트, 탑)
4. 후기/리뷰 (써본 사람, 경험, 평가)
5. 가격/가성비 (저렴한, 무료, 가성비)
6. 특정 사용 사례 (스타트업용, 대기업용, 초보자용)
7. 기능 특화 (기능별 질문)
8. 대안 탐색 (대신, 대체, 비슷한)

JSON 형식으로 반환:
{
  "queries": [
    {
      "query": "생성된 쿼리",
      "type": "쿼리 유형",
      "intent": "검색 의도 설명",
      "expectedBrandMentionLikelihood": "high/medium/low"
    }
  ]
}`;
```

#### 1.3 데이터베이스 스키마 확장
```sql
-- 파생 쿼리 추적 테이블
CREATE TABLE query_expansions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  scan_id UUID REFERENCES scans(id) ON DELETE CASCADE,
  original_query TEXT NOT NULL,
  derived_query TEXT NOT NULL,
  query_type VARCHAR(50) NOT NULL,
  intent_description TEXT,
  relevance_score DECIMAL(3,2),
  brand_mentioned BOOLEAN,
  mention_position INTEGER,
  provider_results JSONB, -- 각 프로바이더별 결과 요약
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_query_expansions_scan ON query_expansions(scan_id);
CREATE INDEX idx_query_expansions_type ON query_expansions(query_type);
```

---

### Phase 2: 다중 시나리오 테스트 (1주)

#### 2.1 시나리오 정의
```typescript
// src/services/scenarios/types.ts
interface TestScenario {
  id: string;
  name: string;
  description: string;
  queryTemplates: string[];
  targetPersona: PersonaType;
  expectedOutcome: string;
}

type PersonaType = 
  | 'first_time_buyer'    // 처음 구매하는 사람
  | 'comparing_options'   // 비교 쇼핑 중
  | 'price_sensitive'     // 가격 민감
  | 'feature_focused'     // 기능 중시
  | 'brand_aware'         // 이미 브랜드 인지
  | 'problem_solving';    // 문제 해결 목적
```

#### 2.2 시나리오 매트릭스 실행
```
┌─────────────────────────────────────────────────────────────┐
│                    시나리오 매트릭스                          │
├─────────────┬─────────────┬─────────────┬─────────────────────┤
│   쿼리 유형   │   ChatGPT   │   Gemini    │   Perplexity      │
├─────────────┼─────────────┼─────────────┼─────────────────────┤
│ 추천 요청    │ ✅ 1위 언급  │ ✅ 2위 언급  │ ✅ 인용과 함께 언급 │
│ 비교 요청    │ ✅ 긍정 비교 │ ⚠️ 중립     │ ✅ 상세 비교       │
│ 순위 질문    │ ❌ 미언급    │ ✅ 3위      │ ✅ 4위            │
│ 가격 질문    │ ✅ 가성비 강조│ ❌ 미언급   │ ⚠️ 경쟁사 우세    │
│ 후기 질문    │ ⚠️ 일반적   │ ✅ 긍정적   │ ✅ 실제 후기 인용  │
└─────────────┴─────────────┴─────────────┴─────────────────────┘
```

#### 2.3 배치 스캔 기능
```typescript
// src/services/batch-scan/index.ts
interface BatchScanConfig {
  brandId: string;
  baseQuery: string;
  expansionLevel: 'minimal' | 'standard' | 'comprehensive';
  providers: ProviderType[];
  maxQueries: number;
  parallelism: number;
}

interface BatchScanResult {
  scanId: string;
  totalQueries: number;
  completedQueries: number;
  aggregatedMetrics: AggregatedMetrics;
  queryResults: QueryResult[];
  executionTimeMs: number;
}
```

---

### Phase 3: 키워드 노출량 정량화 (1주)

#### 3.1 노출 점수 계산 공식
```typescript
// src/services/exposure-scoring/index.ts

interface ExposureScore {
  keyword: string;
  overallScore: number;      // 0-100
  components: {
    mentionFrequency: number;  // 언급 빈도 (40%)
    positionScore: number;      // 위치 점수 (30%)
    sentimentScore: number;     // 감성 점수 (15%)
    prominenceScore: number;    // 돋보임 정도 (15%)
  };
  breakdown: ProviderBreakdown[];
  trend: TrendIndicator;
}

// 노출 점수 계산
function calculateExposureScore(data: ExposureData): number {
  const weights = {
    mentionFrequency: 0.40,
    positionScore: 0.30,
    sentimentScore: 0.15,
    prominenceScore: 0.15,
  };

  // 언급 빈도 점수 (언급된 프로바이더 수 / 전체 프로바이더 수)
  const mentionScore = (data.mentionedProviders / data.totalProviders) * 100;

  // 위치 점수 (1위=100, 2위=80, 3위=60, 4위=40, 5위+=20)
  const positionScore = calculatePositionScore(data.positions);

  // 감성 점수 (긍정=100, 중립=50, 부정=0)
  const sentimentScore = calculateSentimentScore(data.sentiments);

  // 돋보임 점수 (primary=100, secondary=70, mentioned=40)
  const prominenceScore = calculateProminenceScore(data.prominences);

  return (
    mentionScore * weights.mentionFrequency +
    positionScore * weights.positionScore +
    sentimentScore * weights.sentimentScore +
    prominenceScore * weights.prominenceScore
  );
}
```

#### 3.2 키워드 추적 테이블
```sql
-- 키워드별 노출 추적
CREATE TABLE keyword_exposure (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  brand_id UUID REFERENCES brands(id) ON DELETE CASCADE,
  scan_id UUID REFERENCES scans(id) ON DELETE CASCADE,
  keyword TEXT NOT NULL,
  exposure_score DECIMAL(5,2) NOT NULL,
  mention_count INTEGER DEFAULT 0,
  avg_position DECIMAL(3,1),
  sentiment_distribution JSONB, -- {positive: 3, neutral: 2, negative: 0}
  provider_scores JSONB,        -- {openai: 85, gemini: 70, ...}
  recorded_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(brand_id, scan_id, keyword)
);

CREATE INDEX idx_keyword_exposure_brand ON keyword_exposure(brand_id);
CREATE INDEX idx_keyword_exposure_date ON keyword_exposure(recorded_at);
CREATE INDEX idx_keyword_exposure_keyword ON keyword_exposure(keyword);
```

#### 3.3 키워드 대시보드 컴포넌트
```typescript
// src/components/dashboard/keyword-exposure.tsx
interface KeywordExposureProps {
  keywords: KeywordExposure[];
  timeRange: '7d' | '30d' | '90d';
}

// 키워드별 노출 히트맵
// ┌──────────────┬────────┬────────┬────────┬────────┬──────────┐
// │    키워드     │ ChatGPT│ Gemini │ Claude │  Grok  │Perplexity│
// ├──────────────┼────────┼────────┼────────┼────────┼──────────┤
// │ SaaS 도구    │ 🟢 95  │ 🟢 88  │ 🟡 65  │ 🔴 30  │ 🟢 92   │
// │ 협업 툴      │ 🟡 72  │ 🟢 85  │ 🟢 90  │ 🟡 55  │ 🟢 88   │
// │ 프로젝트관리  │ 🟢 88  │ 🟡 60  │ 🟡 58  │ 🔴 25  │ 🟡 70   │
// └──────────────┴────────┴────────┴────────┴────────┴──────────┘
```

---

### Phase 4: 시계열 대시보드 (1주)

#### 4.1 시간별 추이 시각화
```typescript
// src/components/analytics/visibility-timeline.tsx
interface TimelineData {
  date: string;
  overallScore: number;
  aiScore: number;
  seoScore: number;
  geoScore: number;
  events?: TimelineEvent[]; // 특이사항 마커
}

interface TimelineEvent {
  date: string;
  type: 'content_published' | 'campaign_started' | 'competitor_action' | 'algorithm_update';
  description: string;
  impact: 'positive' | 'negative' | 'neutral';
}
```

#### 4.2 노력 대비 효과 측정
```typescript
// 마케팅 활동과 가시성 변화 연관 분석
interface EffortTracking {
  activities: MarketingActivity[];
  visibilityChanges: VisibilityChange[];
  correlation: CorrelationAnalysis;
}

interface MarketingActivity {
  id: string;
  date: string;
  type: 'content' | 'pr' | 'social' | 'seo_update' | 'paid_campaign';
  description: string;
  investment?: number; // 비용 (선택)
}

// 활동 후 가시성 변화 추적
// "블로그 게시 후 2주: Gemini에서 언급 시작 (+15점)"
// "FAQ 페이지 추가 후 1주: ChatGPT 응답 품질 개선 (+8점)"
```

#### 4.3 벤치마크 비교
```typescript
// src/services/benchmarking/index.ts
interface BenchmarkData {
  brandScore: number;
  industryAverage: number;
  industryTop10: number;
  industryTop25: number;
  competitorScores: Record<string, number>;
  percentileRank: number; // 상위 몇 %인지
}

// 시각화: 산업 평균 대비 위치
// ├──────────────────────────────────────────────────────┤
// │ 산업 하위        평균        상위 25%    상위 10%      │
// │    ↓              ↓            ↓           ↓         │
// │ ───●─────────────●────────────●───────────●─────── │
// │                          ▲                          │
// │                       내 브랜드 (72점, 상위 18%)     │
// └──────────────────────────────────────────────────────┘
```

---

### Phase 5: 종합 리포트 시스템 (1주)

#### 5.1 리포트 구조
```typescript
// src/services/reports/comprehensive-report.ts
interface ComprehensiveReport {
  // 헤더
  header: {
    brandName: string;
    reportPeriod: { start: string; end: string };
    generatedAt: string;
  };

  // 요약 (Executive Summary)
  executiveSummary: {
    overallScore: number;
    scoreChange: number;
    keyFindings: string[];
    topRecommendations: string[];
  };

  // 섹션별 상세
  sections: {
    // 1. 가시성 개요
    visibilityOverview: {
      currentScore: number;
      previousScore: number;
      trend: TrendData;
      providerBreakdown: ProviderScore[];
    };

    // 2. 키워드 분석
    keywordAnalysis: {
      topPerformingKeywords: KeywordPerformance[];
      underperformingKeywords: KeywordPerformance[];
      newOpportunities: string[];
    };

    // 3. 경쟁사 분석
    competitorAnalysis: {
      shareOfVoice: ShareOfVoice[];
      competitorStrengths: CompetitorInsight[];
      gapsToAddress: string[];
    };

    // 4. 프로바이더별 분석
    providerDeepDive: {
      [provider: string]: {
        score: number;
        mentions: number;
        sentiment: SentimentBreakdown;
        recommendations: string[];
      };
    };

    // 5. 개선 권장사항
    recommendations: {
      immediate: Recommendation[];    // 즉시 실행
      shortTerm: Recommendation[];    // 1-2주 내
      longTerm: Recommendation[];     // 1-3개월
    };

    // 6. 다음 단계
    nextSteps: {
      prioritizedActions: Action[];
      scheduledScans: ScheduledScan[];
    };
  };
}
```

#### 5.2 리포트 출력 형식
```typescript
// 지원 형식
type ReportFormat = 'pdf' | 'html' | 'markdown' | 'json';

// PDF 생성 (react-pdf 또는 puppeteer)
// 이메일 발송 (Resend/SendGrid)
// Slack 알림 (요약본)
```

#### 5.3 자동 리포트 스케줄링
```typescript
// Inngest cron job
export const weeklyReportFunction = inngest.createFunction(
  { id: 'weekly-visibility-report', name: 'Weekly Visibility Report' },
  { cron: 'TZ=Asia/Seoul 0 9 * * 1' }, // 매주 월요일 오전 9시
  async ({ step }) => {
    // 1. 지난 7일 데이터 수집
    // 2. 리포트 생성
    // 3. 이메일 발송
    // 4. Slack 알림
  }
);
```

---

### Phase 6: UI/UX 개선 (1주)

#### 6.1 새로운 스캔 플로우
```
현재: 단일 쿼리 입력 → 스캔 → 결과

개선:
┌─────────────────────────────────────────────────────────────┐
│                    🔍 새 스캔 시작                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  기본 쿼리 입력:                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ SaaS 소프트웨어 추천해줘                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [✓] 파생 쿼리 자동 생성 (8-12개)                           │
│  [✓] 모든 AI 프로바이더 테스트                              │
│  [ ] 경쟁사 포함 비교 분석                                   │
│                                                             │
│  예상 크레딧: 24 | 예상 시간: 2-3분                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 📝 생성될 파생 쿼리 미리보기                           │  │
│  │ • SaaS 소프트웨어 비교해줘                             │  │
│  │ • 스타트업용 SaaS 도구 추천                            │  │
│  │ • {브랜드명} vs {경쟁사} 어떤 게 나아?                 │  │
│  │ • 2024년 SaaS 순위 알려줘                             │  │
│  │ ... +4개 더보기                                       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│            [ 취소 ]  [ 🚀 스캔 시작 ]                        │
└─────────────────────────────────────────────────────────────┘
```

#### 6.2 결과 대시보드 재설계
```
┌─────────────────────────────────────────────────────────────────┐
│  FantaScan AI                        🔔  ⚙️  👤                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────┐  ┌───────────────────────────────────┐  │
│  │   Overall Score   │  │        시간별 추이 차트              │  │
│  │      ┌───┐        │  │  100│    ╭──╮                      │  │
│  │      │78 │        │  │   80│╭──╯  ╰──╮    ╭──╮            │  │
│  │      └───┘        │  │   60│          ╰──╯  ╰──╮          │  │
│  │   +12 vs 지난주    │  │   40│                     ╰──       │  │
│  │                   │  │     └─────────────────────────────  │  │
│  │  AI: 82  SEO: 74  │  │      1주전        현재              │  │
│  └───────────────────┘  └───────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              키워드 노출 히트맵                            │   │
│  │  ┌────────────┬────────┬────────┬────────┬──────────┐   │   │
│  │  │            │ChatGPT │ Gemini │ Claude │Perplexity│   │   │
│  │  ├────────────┼────────┼────────┼────────┼──────────┤   │   │
│  │  │ 메인 키워드 │ 🟢 92  │ 🟢 88  │ 🟡 65  │ 🟢 95   │   │   │
│  │  │ 서브 키워드 │ 🟡 68  │ 🟢 82  │ 🔴 35  │ 🟢 78   │   │   │
│  │  └────────────┴────────┴────────┴────────┴──────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────┐  ┌─────────────────────────────────┐  │
│  │  Share of Voice     │  │     우선 개선 권장사항             │  │
│  │  ┌─────────────┐    │  │  🔴 Claude 가시성 개선 필요       │  │
│  │  │ 내 브랜드 42%│    │  │  🟡 FAQ 구조화 데이터 추가        │  │
│  │  │ 경쟁A   28%  │    │  │  🟢 Perplexity 인용 유지         │  │
│  │  │ 경쟁B   18%  │    │  │                                  │  │
│  │  │ 기타    12%  │    │  │  [상세 보기]                     │  │
│  │  └─────────────┘    │  └─────────────────────────────────┘  │
│  └─────────────────────┘                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗓️ 구현 일정

| 주차 | Phase | 주요 작업 | 산출물 |
|------|-------|----------|--------|
| Week 1 | Phase 1 | 파생 쿼리 자동 생성 | query-expansion 서비스, DB 스키마 |
| Week 2 | Phase 2 | 다중 시나리오 테스트 | batch-scan 기능, 시나리오 매트릭스 |
| Week 3 | Phase 3 | 키워드 노출량 정량화 | exposure-scoring 서비스, 키워드 대시보드 |
| Week 4 | Phase 4 | 시계열 대시보드 | 트렌드 차트, 벤치마크 비교 |
| Week 5 | Phase 5 | 종합 리포트 시스템 | PDF 생성, 이메일 발송, 스케줄링 |
| Week 6 | Phase 6 | UI/UX 개선 | 새 스캔 플로우, 대시보드 재설계 |

---

## 📁 파일 구조 (추가/수정)

```
src/
├── services/
│   ├── query-expansion/          # 🆕 파생 쿼리 생성
│   │   ├── index.ts
│   │   ├── llm-expander.ts
│   │   └── template-expander.ts
│   ├── batch-scan/               # 🆕 배치 스캔
│   │   ├── index.ts
│   │   └── scenario-matrix.ts
│   ├── exposure-scoring/         # 🆕 노출 점수 계산
│   │   ├── index.ts
│   │   └── keyword-tracking.ts
│   ├── benchmarking/             # 🆕 벤치마크
│   │   └── index.ts
│   └── reports/
│       ├── index.ts
│       ├── comprehensive-report.ts  # 🆕 종합 리포트
│       └── pdf-generator.ts         # 🆕 PDF 생성
├── components/
│   ├── scan/
│   │   ├── new-scan-wizard.tsx      # 🆕 새 스캔 마법사
│   │   └── derived-queries-preview.tsx  # 🆕 파생 쿼리 미리보기
│   ├── dashboard/
│   │   ├── keyword-heatmap.tsx      # 🆕 키워드 히트맵
│   │   ├── visibility-timeline.tsx  # 🆕 시계열 차트
│   │   └── benchmark-comparison.tsx # 🆕 벤치마크 비교
│   └── reports/
│       └── report-viewer.tsx        # 🆕 리포트 뷰어
└── app/
    └── api/
        ├── scans/
        │   └── batch/route.ts       # 🆕 배치 스캔 API
        ├── keywords/
        │   └── exposure/route.ts    # 🆕 키워드 노출 API
        └── reports/
            └── generate/route.ts    # 🆕 리포트 생성 API
```

---

## 🔧 기술 스택 추가

| 기능 | 라이브러리 | 용도 |
|------|-----------|------|
| PDF 생성 | `@react-pdf/renderer` 또는 `puppeteer` | 리포트 PDF 출력 |
| 차트 | `recharts` (기존) + `visx` | 고급 시각화 |
| 이메일 | `resend` 또는 `@sendgrid/mail` | 리포트 발송 |
| 스케줄링 | `inngest` (기존) | 자동 스캔/리포트 |

---

## 📊 성공 지표 (KPI)

| 지표 | 현재 | 목표 |
|------|------|------|
| 단일 스캔당 쿼리 수 | 1 | 8-12 (파생 쿼리 포함) |
| 키워드 추적 가능 여부 | ❌ | ✅ |
| 시계열 데이터 보존 | 7일 | 90일+ |
| 리포트 형식 | 텍스트만 | PDF, HTML, 이메일 |
| 경쟁사 벤치마크 | 기본 SOV | 산업 평균 + 상위% |

---

## 🚀 즉시 시작 가능한 작업

1. **DB 마이그레이션 작성**: `query_expansions`, `keyword_exposure` 테이블
2. **Query Expansion 서비스 구현**: 가장 핵심 기능
3. **배치 스캔 Inngest 함수 수정**: 파생 쿼리 지원
4. **키워드 히트맵 컴포넌트**: 시각화

---

*문서 작성일: 2026-02-04*
*작성자: Clawdbot*
