# FantaScan AI - 배치 스캔 시스템 구현 계획서

*작성일: 2026-02-06*
*상태: 진행 예정*

---

## 📋 목차

1. [개요](#1-개요)
2. [요구사항 정리](#2-요구사항-정리)
3. [데이터베이스 스키마](#3-데이터베이스-스키마)
4. [질문 세트 시스템](#4-질문-세트-시스템)
5. [프로바이더 및 반복 설정](#5-프로바이더-및-반복-설정)
6. [스캔 실행 시스템](#6-스캔-실행-시스템)
7. [예외 처리 및 재개 기능](#7-예외-처리-및-재개-기능)
8. [대시보드 UX 개선](#8-대시보드-ux-개선)
9. [구현 체크리스트](#9-구현-체크리스트)

---

## 1. 개요

### 1.1 현재 상태
- 단일 질문을 직접 입력하여 스캔
- 5개 AI 프로바이더 (Gemini, GPT, Claude, Grok, Perplexity)
- 질문당 1회 API 호출

### 1.2 개선 목표
- 질문 세트 기반 다중 질문 스캔
- Gemini, GPT 2개 프로바이더만 사용
- 질문당 설정된 횟수만큼 반복 (기본 50회씩)
- 진행 상황 추적 및 중단/재개 기능
- 질문별 AI 가시성 노출도 측정

---

## 2. 요구사항 정리

| # | 요구사항 | 상세 |
|---|---------|------|
| 1 | 질문 세트 | 여러 질문을 세트로 묶어서 한번에 실행 |
| 2 | 프로바이더 변경 | Gemini, GPT만 사용 (50회씩, 설정 가능) |
| 3 | 스캔 전 미리보기 | 질문 목록, 예상 시간, 예상 크레딧 표시 |
| 4 | 진행 상황 추적 | 질문별 진행률, 체크마크, 중단 후 재개 가능 |
| 5 | 크레딧 시스템 | API 콜당 크레딧, 스캔 전 소모량 표시 |
| 6 | 대시보드 개선 | AI 가시성 노출도(%), 질문별 뷰 |
| 7 | 분석 데이터 검증 | 기존 분석 기능 정상 동작 확인 |
| 8 | 가시성 추이 | 질문 세트/질문별 추이 UX |
| 9 | 경쟁사 점유율 | 설정된 경쟁사만 체크 |
| 10 | 인용 소스 | API 응답에 인용 URL 있을 때만 표시 |
| 11 | 개선 권장사항 | 경쟁사보다 낮을 때 간단 권장 |
| 12 | 스캔 히스토리 | 대량 스캔 이력 표시 UX |
| 13 | Inngest 진행 관리 | API 타임아웃, 진행 상황 저장 |
| 14 | 예외 처리 | 중단/재개, 에러 복구 |

### 2.1 스캔 플로우 변경사항
- ❌ 브랜드 선택 제거 (설정에서 이미 선택된 브랜드 사용)
- ✅ 질문 세트 선택으로 변경

---

## 3. 데이터베이스 스키마

### 3.1 새로운 테이블

```sql
-- ============================================
-- 3.1.1 질문 세트 테이블
-- ============================================
CREATE TABLE question_sets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,           -- "SaaS 추천 세트"
  description TEXT,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_question_sets_user ON question_sets(user_id);
CREATE INDEX idx_question_sets_active ON question_sets(user_id, is_active);

-- ============================================
-- 3.1.2 질문 세트 내 개별 질문
-- ============================================
CREATE TABLE question_set_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  question_set_id UUID REFERENCES question_sets(id) ON DELETE CASCADE,
  question_text TEXT NOT NULL,          -- "SaaS 추천해줘"
  order_index INTEGER NOT NULL,         -- 정렬 순서
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_question_set_items_set ON question_set_items(question_set_id);
CREATE INDEX idx_question_set_items_order ON question_set_items(question_set_id, order_index);

-- ============================================
-- 3.1.3 사용자 스캔 설정
-- ============================================
CREATE TABLE user_scan_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE UNIQUE,
  
  -- 프로바이더별 반복 횟수
  gemini_iterations INTEGER DEFAULT 50,
  openai_iterations INTEGER DEFAULT 50,
  
  -- 타임아웃 설정
  timeout_per_call_ms INTEGER DEFAULT 30000,  -- 30초
  
  -- 기본 브랜드 (스캔 시 자동 사용)
  default_brand_id UUID REFERENCES brands(id) ON DELETE SET NULL,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE UNIQUE INDEX idx_user_scan_settings_user ON user_scan_settings(user_id);

-- ============================================
-- 3.1.4 배치 스캔 (질문 세트 단위 스캔)
-- ============================================
CREATE TABLE batch_scans (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  brand_id UUID REFERENCES brands(id) ON DELETE CASCADE,
  question_set_id UUID REFERENCES question_sets(id),
  
  -- 상태: pending, running, paused, completed, failed
  status VARCHAR(20) DEFAULT 'pending',
  pause_reason VARCHAR(100),            -- network_error, insufficient_credits, user_paused
  
  -- 진행 상황
  total_questions INTEGER NOT NULL,
  completed_questions INTEGER DEFAULT 0,
  total_iterations INTEGER NOT NULL,    -- 전체 API 콜 수
  completed_iterations INTEGER DEFAULT 0,
  
  -- 크레딧
  estimated_credits INTEGER NOT NULL,
  used_credits INTEGER DEFAULT 0,
  
  -- 결과 집계
  overall_exposure_rate DECIMAL(5,2),   -- 전체 노출률 (%)
  
  -- 시간
  estimated_duration_ms BIGINT,
  started_at TIMESTAMPTZ,
  paused_at TIMESTAMPTZ,
  resumed_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_batch_scans_user ON batch_scans(user_id);
CREATE INDEX idx_batch_scans_brand ON batch_scans(brand_id);
CREATE INDEX idx_batch_scans_status ON batch_scans(status);
CREATE INDEX idx_batch_scans_created ON batch_scans(user_id, created_at DESC);

-- ============================================
-- 3.1.5 배치 스캔 내 개별 질문 진행 상황
-- ============================================
CREATE TABLE batch_scan_questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_scan_id UUID REFERENCES batch_scans(id) ON DELETE CASCADE,
  question_set_item_id UUID REFERENCES question_set_items(id),
  question_text TEXT NOT NULL,
  question_order INTEGER NOT NULL,
  
  -- 상태: pending, running, completed, failed
  status VARCHAR(20) DEFAULT 'pending',
  
  -- 프로바이더별 진행 상황
  gemini_completed INTEGER DEFAULT 0,
  gemini_total INTEGER NOT NULL,
  openai_completed INTEGER DEFAULT 0,
  openai_total INTEGER NOT NULL,
  
  -- 결과 집계 (언급 횟수)
  gemini_mention_count INTEGER DEFAULT 0,
  openai_mention_count INTEGER DEFAULT 0,
  
  -- 노출률 (%)
  gemini_exposure_rate DECIMAL(5,2),
  openai_exposure_rate DECIMAL(5,2),
  avg_exposure_rate DECIMAL(5,2),
  
  -- 경쟁사 언급 집계
  competitor_mentions JSONB DEFAULT '{}',  -- {"Notion": 45, "Slack": 12}
  
  -- 감성 집계
  sentiment_positive INTEGER DEFAULT 0,
  sentiment_neutral INTEGER DEFAULT 0,
  sentiment_negative INTEGER DEFAULT 0,
  
  -- 에러 추적
  last_error TEXT,
  retry_count INTEGER DEFAULT 0,
  
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_batch_scan_questions_batch ON batch_scan_questions(batch_scan_id);
CREATE INDEX idx_batch_scan_questions_status ON batch_scan_questions(batch_scan_id, status);

-- ============================================
-- 3.1.6 개별 API 콜 결과 (iteration별)
-- ============================================
CREATE TABLE batch_scan_iterations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_scan_question_id UUID REFERENCES batch_scan_questions(id) ON DELETE CASCADE,
  provider VARCHAR(20) NOT NULL,         -- 'gemini' | 'openai'
  iteration_index INTEGER NOT NULL,      -- 1~50
  
  -- 상태: pending, success, failed, timeout
  status VARCHAR(20) DEFAULT 'pending',
  
  -- 결과
  response_text TEXT,
  brand_mentioned BOOLEAN,
  mention_position INTEGER,              -- null if not mentioned, 1-5 position
  sentiment VARCHAR(20),                 -- positive, neutral, negative
  competitors_mentioned JSONB,           -- {"Notion": true, "Slack": false}
  
  -- 인용 (있는 경우만)
  citations JSONB,                       -- [{"url": "...", "title": "..."}]
  
  -- 메타
  response_time_ms INTEGER,
  error_message TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_iterations_question ON batch_scan_iterations(batch_scan_question_id);
CREATE INDEX idx_iterations_provider ON batch_scan_iterations(batch_scan_question_id, provider);
CREATE INDEX idx_iterations_status ON batch_scan_iterations(batch_scan_question_id, status);

-- ============================================
-- 3.2 기존 테이블 수정
-- ============================================

-- users 테이블에 기본 브랜드 추가 (이미 있다면 스킵)
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS primary_brand_id UUID REFERENCES brands(id);
```

### 3.2 마이그레이션 파일 경로
`supabase/migrations/20260206_add_batch_scan_tables.sql`

---

## 4. 질문 세트 시스템

### 4.1 파일 구조

```
src/
├── app/
│   ├── api/
│   │   └── question-sets/
│   │       ├── route.ts              # GET (목록), POST (생성)
│   │       └── [id]/
│   │           ├── route.ts          # GET, PATCH, DELETE
│   │           └── items/
│   │               └── route.ts      # POST (질문 추가)
│   └── (app)/
│       └── settings/
│           └── question-sets/
│               └── page.tsx          # 질문 세트 관리 페이지
├── components/
│   └── question-sets/
│       ├── question-set-list.tsx     # 세트 목록
│       ├── question-set-card.tsx     # 개별 세트 카드
│       ├── question-set-form.tsx     # 생성/편집 폼
│       └── question-item.tsx         # 개별 질문 항목
├── hooks/
│   └── use-question-sets.ts          # SWR 훅
└── types/
    └── question-set.ts               # 타입 정의
```

### 4.2 API 엔드포인트

```typescript
// GET /api/question-sets
// 사용자의 질문 세트 목록 조회
Response: {
  sets: QuestionSet[]
}

// POST /api/question-sets
// 새 질문 세트 생성
Request: {
  name: string
  description?: string
  questions: string[]
}
Response: {
  set: QuestionSet
}

// GET /api/question-sets/:id
// 단일 세트 상세 조회 (질문 포함)
Response: {
  set: QuestionSet & { items: QuestionSetItem[] }
}

// PATCH /api/question-sets/:id
// 세트 수정
Request: {
  name?: string
  description?: string
  is_active?: boolean
}

// DELETE /api/question-sets/:id
// 세트 삭제

// POST /api/question-sets/:id/items
// 질문 추가
Request: {
  question_text: string
  order_index?: number
}

// PATCH /api/question-sets/:id/items/:itemId
// 질문 수정

// DELETE /api/question-sets/:id/items/:itemId
// 질문 삭제
```

### 4.3 UI 와이어프레임

#### 질문 세트 관리 페이지 (`/settings/question-sets`)

```
┌─────────────────────────────────────────────────────────────┐
│  📝 질문 세트 관리                         [+ 새 세트 만들기]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🗂️ SaaS 추천 질문 세트                    [편집] [삭제] │   │
│  │ 질문 5개 | 마지막 사용: 2일 전                        │   │
│  │ ├─ "SaaS 소프트웨어 추천해줘"                        │   │
│  │ ├─ "스타트업용 협업 툴 추천"                         │   │
│  │ ├─ "{브랜드명} vs 노션 비교"                         │   │
│  │ ├─ "2024년 프로젝트 관리 툴 순위"                    │   │
│  │ └─ "{브랜드명} 사용 후기"                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🗂️ 경쟁사 비교 질문 세트                  [편집] [삭제] │   │
│  │ 질문 3개 | 마지막 사용: 1주 전                        │   │
│  │ [펼치기 ▾]                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 질문 세트 생성/편집 모달

```
┌─────────────────────────────────────────────────────────────┐
│  새 질문 세트 만들기                                    [X]  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  세트 이름 *                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ SaaS 추천 질문 세트                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  설명 (선택)                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ SaaS 관련 추천 질문 모음                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  질문 목록                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ≡ 1. SaaS 소프트웨어 추천해줘                  [🗑️]  │   │
│  │ ≡ 2. 스타트업용 협업 툴 추천                   [🗑️]  │   │
│  │ ≡ 3. {브랜드명} vs 노션 비교                   [🗑️]  │   │
│  └─────────────────────────────────────────────────────┘   │
│  (드래그하여 순서 변경 가능)                                 │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 새 질문 입력...                              [+ 추가] │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  💡 팁: {브랜드명}, {경쟁사} 변수를 사용하면                  │
│        스캔 시 실제 값으로 자동 치환됩니다.                   │
│                                                             │
│                        [ 취소 ]  [ 저장 ]                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 프로바이더 및 반복 설정

### 5.1 설정 페이지 확장 (`/settings`)

```
┌─────────────────────────────────────────────────────────────┐
│  ⚙️ 스캔 설정                                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🎯 기본 브랜드                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ▼ MyBrand                                           │   │
│  └─────────────────────────────────────────────────────┘   │
│  스캔 시 이 브랜드가 자동으로 선택됩니다.                      │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  🤖 AI 프로바이더별 반복 횟수                                 │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Gemini                                             │   │
│  │  ┌──────────────────────────────────────────┐      │   │
│  │  │ 50                                       │ 회   │   │
│  │  └──────────────────────────────────────────┘      │   │
│  │  질문당 Gemini에 50회 질문합니다.                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  GPT-4                                              │   │
│  │  ┌──────────────────────────────────────────┐      │   │
│  │  │ 50                                       │ 회   │   │
│  │  └──────────────────────────────────────────┘      │   │
│  │  질문당 GPT에 50회 질문합니다.                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  ⏱️ API 타임아웃                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 30                                              │ 초 │   │
│  └─────────────────────────────────────────────────────┘   │
│  각 API 호출의 최대 대기 시간입니다.                          │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  💰 크레딧 안내                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • Gemini: 1 크레딧/회                               │   │
│  │ • GPT-4:  2 크레딧/회                               │   │
│  │                                                    │   │
│  │ 📊 예시 계산:                                       │   │
│  │ 5개 질문 × (50회 Gemini + 50회 GPT)                 │   │
│  │ = 5 × (50×1 + 50×2) = 750 크레딧                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                              [ 저장 ]                       │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 크레딧 계산 유틸리티

```typescript
// src/lib/credits.ts

export const CREDIT_COSTS = {
  gemini: 1,
  openai: 2,
} as const;

export type Provider = keyof typeof CREDIT_COSTS;

export interface CreditEstimate {
  geminiCredits: number;
  openaiCredits: number;
  totalCredits: number;
  totalIterations: number;
}

export function calculateEstimatedCredits(
  questionCount: number,
  geminiIterations: number,
  openaiIterations: number
): CreditEstimate {
  const geminiCredits = questionCount * geminiIterations * CREDIT_COSTS.gemini;
  const openaiCredits = questionCount * openaiIterations * CREDIT_COSTS.openai;
  
  return {
    geminiCredits,
    openaiCredits,
    totalCredits: geminiCredits + openaiCredits,
    totalIterations: questionCount * (geminiIterations + openaiIterations),
  };
}

export interface DurationEstimate {
  minDurationMs: number;
  maxDurationMs: number;
  avgDurationMs: number;
  formattedMin: string;
  formattedMax: string;
}

export function calculateEstimatedDuration(
  totalIterations: number,
  avgResponseTimeMs: number = 3000,
  parallelism: number = 5
): DurationEstimate {
  const parallelBatches = Math.ceil(totalIterations / parallelism);
  const avgDurationMs = parallelBatches * avgResponseTimeMs;
  
  return {
    minDurationMs: avgDurationMs * 0.7,
    maxDurationMs: avgDurationMs * 1.5,
    avgDurationMs,
    formattedMin: formatDuration(avgDurationMs * 0.7),
    formattedMax: formatDuration(avgDurationMs * 1.5),
  };
}

function formatDuration(ms: number): string {
  const minutes = Math.floor(ms / 60000);
  const seconds = Math.floor((ms % 60000) / 1000);
  
  if (minutes === 0) return `${seconds}초`;
  if (seconds === 0) return `${minutes}분`;
  return `${minutes}분 ${seconds}초`;
}
```

### 5.3 API 엔드포인트

```typescript
// GET /api/settings/scan
// 스캔 설정 조회
Response: {
  settings: UserScanSettings
}

// PATCH /api/settings/scan
// 스캔 설정 수정
Request: {
  gemini_iterations?: number
  openai_iterations?: number
  timeout_per_call_ms?: number
  default_brand_id?: string
}
```

---

## 6. 스캔 실행 시스템

### 6.1 스캔 시작 페이지 (`/scan/new`)

**브랜드 선택 제거** - 설정에서 선택된 기본 브랜드 자동 사용

```
┌─────────────────────────────────────────────────────────────┐
│  🔍 새 스캔 시작                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  브랜드: MyBrand (SaaS)                    [설정에서 변경 →] │
│  경쟁사: Notion, Slack, Asana                               │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📋 질문 세트 선택                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ SaaS 추천 질문 세트 (5개 질문)                     │   │
│  │ ● 경쟁사 비교 질문 세트 (3개 질문)    ← 선택됨       │   │
│  │ ○ 후기/리뷰 질문 세트 (4개 질문)                     │   │
│  └─────────────────────────────────────────────────────┘   │
│  [+ 새 질문 세트 만들기]                                    │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📝 선택된 질문 미리보기                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  #   질문                            Gemini   GPT   │   │
│  │ ─────────────────────────────────────────────────── │   │
│  │  1   MyBrand vs Notion 비교해줘       50회    50회  │   │
│  │  2   MyBrand vs Slack 장단점          50회    50회  │   │
│  │  3   MyBrand 대안 추천해줘            50회    50회  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📊 예상 정보                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  총 API 콜:      300회 (Gemini 150 + GPT 150)       │   │
│  │  예상 시간:      약 15~20분                         │   │
│  │  예상 크레딧:    450 크레딧                          │   │
│  │  보유 크레딧:    1,200 크레딧 ✅ 충분                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⚠️ 스캔은 서버에서 진행되므로 브라우저를 닫아도 계속됩니다.   │
│     언제든지 일시정지하고 나중에 재개할 수 있습니다.          │
│                                                             │
│               [ 취소 ]  [ 🚀 스캔 시작 (450 크레딧) ]        │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 진행 상황 페이지 (`/scan/[batchScanId]`)

```
┌─────────────────────────────────────────────────────────────┐
│  🔄 스캔 진행 중                          [ ⏸️ 일시정지 ]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  전체 진행률                                                 │
│  ████████████░░░░░░░░░░░░░░░░░░░░ 42% (126/300)            │
│                                                             │
│  ⏱️ 경과: 6분 32초 | 예상 남은 시간: ~9분                    │
│  💰 크레딧: 189/450 사용됨                                   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📋 질문별 진행 상황                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  ✅ 질문 1: "MyBrand vs Notion 비교해줘"            │   │
│  │     Gemini: ████████████████████ 50/50 완료        │   │
│  │     GPT:    ████████████████████ 50/50 완료        │   │
│  │     → 노출률: Gemini 72%, GPT 68% | 평균 70%        │   │
│  │                                                     │   │
│  │  🔄 질문 2: "MyBrand vs Slack 장단점"               │   │
│  │     Gemini: ████████████░░░░░░░░ 26/50 진행 중     │   │
│  │     GPT:    ░░░░░░░░░░░░░░░░░░░░ 0/50  대기        │   │
│  │     → 현재 노출률: Gemini 65% (17/26)               │   │
│  │                                                     │   │
│  │  ⏳ 질문 3: "MyBrand 대안 추천해줘"                  │   │
│  │     Gemini: ░░░░░░░░░░░░░░░░░░░░ 0/50  대기        │   │
│  │     GPT:    ░░░░░░░░░░░░░░░░░░░░ 0/50  대기        │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 일시정지 상태

```
┌─────────────────────────────────────────────────────────────┐
│  ⏸️ 스캔 일시정지됨                        [ ▶️ 재개하기 ]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  진행률: ████████████░░░░░░░░░░░░░░░░░░░░ 42% (126/300)    │
│                                                             │
│  ⏱️ 일시정지 시각: 2026-02-06 01:45:32                      │
│  💰 사용된 크레딧: 189 / 잔여 예상: 261                       │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  ℹ️ 재개 정보                                                │
│  • 질문 2의 Gemini 27번째부터 재개됩니다.                     │
│  • 재개 시 남은 261 크레딧이 필요합니다.                      │
│  • 보유 크레딧: 1,011 ✅                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.4 완료 상태

```
┌─────────────────────────────────────────────────────────────┐
│  ✅ 스캔 완료                              [결과 보기 →]     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  경쟁사 비교 질문 세트 | 2026-02-06 14:32                    │
│                                                             │
│  📊 전체 AI 가시성 노출도: 68%                               │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  질문별 결과 요약                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  질문                        Gemini    GPT    평균   │   │
│  │ ─────────────────────────────────────────────────── │   │
│  │  MyBrand vs Notion 비교       72%      68%    70%   │   │
│  │  MyBrand vs Slack 장단점      65%      62%    64%   │   │
│  │  MyBrand 대안 추천            70%      71%    71%   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⏱️ 소요 시간: 18분 32초                                     │
│  💰 사용 크레딧: 450                                         │
│                                                             │
│            [ 대시보드로 이동 ]  [ 새 스캔 시작 ]              │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 Inngest 함수 구조

```typescript
// inngest/functions/batch-scan.ts

import { inngest } from '../client';
import { createClient } from '@supabase/supabase-js';

/**
 * 배치 스캔 시작 이벤트 핸들러
 */
export const batchScanStartFunction = inngest.createFunction(
  {
    id: 'batch-scan-start',
    name: 'Batch Scan Start',
    retries: 3,
    concurrency: { limit: 3, key: 'event.data.userId' },
  },
  { event: 'batch-scan/start' },
  async ({ event, step }) => {
    const { batchScanId, userId } = event.data;
    
    // Step 1: 배치 스캔 정보 로드
    const batchScan = await step.run('load-batch-scan', async () => {
      return await loadBatchScanWithQuestions(batchScanId);
    });
    
    // Step 2: 상태를 'running'으로 업데이트
    await step.run('set-running', async () => {
      await updateBatchScanStatus(batchScanId, 'running', {
        started_at: new Date().toISOString()
      });
    });
    
    // Step 3: 각 질문 순차 처리
    for (const question of batchScan.questions) {
      // 이미 완료된 질문 스킵
      if (question.status === 'completed') continue;
      
      // 일시정지 체크
      const isPaused = await step.run(`check-pause-${question.id}`, async () => {
        return await checkIfPaused(batchScanId);
      });
      
      if (isPaused) {
        return { status: 'paused', lastQuestionId: question.id };
      }
      
      // 질문 처리
      await step.run(`process-question-${question.id}`, async () => {
        await processQuestion(batchScanId, question, batchScan.settings);
      });
    }
    
    // Step 4: 완료 처리
    await step.run('finalize', async () => {
      await finalizeBatchScan(batchScanId);
    });
    
    return { status: 'completed', batchScanId };
  }
);

/**
 * 배치 스캔 재개 이벤트 핸들러
 */
export const batchScanResumeFunction = inngest.createFunction(
  {
    id: 'batch-scan-resume',
    name: 'Batch Scan Resume',
    retries: 3,
  },
  { event: 'batch-scan/resume' },
  async ({ event, step }) => {
    const { batchScanId, userId } = event.data;
    
    // 상태 업데이트 후 start 이벤트 재발행
    await step.run('resume-scan', async () => {
      await updateBatchScanStatus(batchScanId, 'running', {
        resumed_at: new Date().toISOString()
      });
    });
    
    // start 함수가 중단된 지점부터 재개
    await step.sendEvent('trigger-resume', {
      name: 'batch-scan/start',
      data: { batchScanId, userId }
    });
    
    return { status: 'resumed' };
  }
);

/**
 * 개별 질문 처리
 */
async function processQuestion(
  batchScanId: string,
  question: BatchScanQuestion,
  settings: UserScanSettings
) {
  // 질문 상태 업데이트
  await updateQuestionStatus(question.id, 'running');
  
  // Gemini iterations
  for (let i = question.gemini_completed; i < question.gemini_total; i++) {
    // 일시정지 체크 (10회마다)
    if (i % 10 === 0) {
      const paused = await checkIfPaused(batchScanId);
      if (paused) break;
    }
    
    try {
      const result = await queryProviderWithTimeout(
        'gemini',
        question.question_text,
        settings.timeout_per_call_ms
      );
      
      await saveIterationResult(question.id, 'gemini', i, result);
      await updateQuestionProgress(question.id, 'gemini', i + 1);
      await deductCredit(batchScanId, 'gemini');
      
    } catch (error) {
      await handleIterationError(batchScanId, question.id, 'gemini', i, error);
    }
  }
  
  // OpenAI iterations (동일 패턴)
  for (let i = question.openai_completed; i < question.openai_total; i++) {
    // ... 동일한 로직
  }
  
  // 질문 완료 처리
  await finalizeQuestion(question.id);
}
```

### 6.6 API 엔드포인트

```typescript
// POST /api/batch-scans
// 새 배치 스캔 생성 및 시작
Request: {
  question_set_id: string
}
Response: {
  batchScan: BatchScan
}

// GET /api/batch-scans/:id
// 배치 스캔 상세 (진행 상황 포함)
Response: {
  batchScan: BatchScan & {
    questions: BatchScanQuestion[]
  }
}

// POST /api/batch-scans/:id/pause
// 일시정지
Response: {
  success: boolean
}

// POST /api/batch-scans/:id/resume
// 재개
Response: {
  success: boolean
}

// GET /api/batch-scans
// 배치 스캔 히스토리
Query: {
  limit?: number
  offset?: number
}
Response: {
  batchScans: BatchScan[]
  total: number
}
```

---

## 7. 예외 처리 및 재개 기능

### 7.1 예외 상황 목록

| 예외 상황 | 감지 방법 | 대응 방안 | 우선순위 |
|----------|----------|----------|---------|
| API 타임아웃 | AbortController | iteration 실패 처리, 다음으로 진행 | 🔴 |
| Rate Limit (429) | HTTP status | exponential backoff (최대 3회) | 🔴 |
| 서버 에러 (5xx) | HTTP status | 10초 대기 후 재시도 | 🔴 |
| 네트워크 단절 | fetch 에러 | 30초 대기, 3회 실패시 일시정지 | 🔴 |
| 사용자 일시정지 | DB status | 현재 iteration 완료 후 정지 | 🟡 |
| 크레딧 소진 | iteration 전 체크 | 즉시 일시정지, 알림 | 🔴 |
| Inngest 재시작 | 자동 | 마지막 저장 지점부터 재개 | 🟡 |
| 브라우저 닫힘 | N/A | 영향 없음 (서버 처리) | 🟢 |
| 잘못된 API 키 | 401 에러 | 즉시 실패, 사용자 알림 | 🔴 |
| 응답 파싱 에러 | JSON 파싱 실패 | 해당 iteration 실패 처리 | 🟡 |

### 7.2 에러 복구 로직

```typescript
// src/services/batch-scan/error-handler.ts

export type ErrorAction = 'retry' | 'skip' | 'pause' | 'fail';

export interface ErrorClassification {
  type: 'RATE_LIMIT' | 'TIMEOUT' | 'NETWORK' | 'AUTH' | 'SERVER' | 'PARSE' | 'UNKNOWN';
  action: ErrorAction;
  retryDelayMs?: number;
  message: string;
}

export function classifyError(error: Error): ErrorClassification {
  const message = error.message.toLowerCase();
  
  // Rate Limit
  if (message.includes('429') || message.includes('rate limit') || message.includes('quota')) {
    return {
      type: 'RATE_LIMIT',
      action: 'retry',
      retryDelayMs: 60000, // 1분 대기
      message: 'API 요청 한도 초과. 잠시 후 재시도합니다.'
    };
  }
  
  // Timeout
  if (message.includes('timeout') || message.includes('aborted')) {
    return {
      type: 'TIMEOUT',
      action: 'skip',
      message: 'API 응답 시간 초과. 다음으로 진행합니다.'
    };
  }
  
  // Network
  if (message.includes('network') || message.includes('fetch') || message.includes('econnreset')) {
    return {
      type: 'NETWORK',
      action: 'retry',
      retryDelayMs: 30000,
      message: '네트워크 오류. 연결 확인 후 재시도합니다.'
    };
  }
  
  // Auth
  if (message.includes('401') || message.includes('unauthorized') || message.includes('api key')) {
    return {
      type: 'AUTH',
      action: 'fail',
      message: 'API 인증 실패. 설정을 확인해주세요.'
    };
  }
  
  // Server Error
  if (message.includes('500') || message.includes('502') || message.includes('503')) {
    return {
      type: 'SERVER',
      action: 'retry',
      retryDelayMs: 10000,
      message: '서버 오류. 잠시 후 재시도합니다.'
    };
  }
  
  // Parse Error
  if (message.includes('json') || message.includes('parse')) {
    return {
      type: 'PARSE',
      action: 'skip',
      message: '응답 파싱 실패. 다음으로 진행합니다.'
    };
  }
  
  return {
    type: 'UNKNOWN',
    action: 'skip',
    message: `알 수 없는 오류: ${error.message}`
  };
}

export async function handleIterationError(
  batchScanId: string,
  questionId: string,
  provider: string,
  iterationIndex: number,
  error: Error
): Promise<ErrorAction> {
  const classification = classifyError(error);
  
  // 에러 로그 저장
  await saveIterationError(questionId, provider, iterationIndex, {
    type: classification.type,
    message: classification.message,
    originalError: error.message,
  });
  
  switch (classification.action) {
    case 'retry':
      const retryCount = await incrementRetryCount(questionId);
      if (retryCount >= 3) {
        // 3회 이상 실패시 일시정지
        await pauseBatchScan(batchScanId, classification.type);
        return 'pause';
      }
      if (classification.retryDelayMs) {
        await sleep(classification.retryDelayMs);
      }
      return 'retry';
      
    case 'fail':
      await failBatchScan(batchScanId, classification.message);
      return 'fail';
      
    case 'skip':
    default:
      return 'skip';
  }
}
```

### 7.3 재개 API

```typescript
// src/app/api/batch-scans/[id]/resume/route.ts

export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  const batchScanId = params.id;
  
  // 1. 인증 확인
  const user = await getAuthUser();
  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // 2. 배치 스캔 조회
  const batchScan = await getBatchScan(batchScanId);
  if (!batchScan || batchScan.user_id !== user.id) {
    return NextResponse.json({ error: 'Not found' }, { status: 404 });
  }
  
  // 3. 상태 확인
  if (batchScan.status !== 'paused') {
    return NextResponse.json({ 
      error: 'Scan is not paused',
      currentStatus: batchScan.status 
    }, { status: 400 });
  }
  
  // 4. 남은 크레딧 계산
  const remainingCredits = batchScan.estimated_credits - batchScan.used_credits;
  const userCredits = await getUserCredits(user.id);
  
  if (userCredits < remainingCredits) {
    return NextResponse.json({ 
      error: 'Insufficient credits',
      required: remainingCredits,
      available: userCredits 
    }, { status: 402 });
  }
  
  // 5. Inngest 이벤트 발행
  await inngest.send({
    name: 'batch-scan/resume',
    data: { 
      batchScanId,
      userId: user.id 
    }
  });
  
  return NextResponse.json({ 
    success: true,
    message: 'Scan resumed'
  });
}
```

---

## 8. 대시보드 UX 개선

### 8.1 메인 대시보드 (`/dashboard`)

**제거 항목:**
- ❌ AI 제공자별 점수 (Gemini, GPT, Claude 등 개별 표시)
- ❌ 추천 질문 섹션

**변경 항목:**
- "AI 가시성 점수" → "AI 가시성 노출도 (%)"
- 질문별 노출도 테이블 추가

```
┌─────────────────────────────────────────────────────────────┐
│  📊 MyBrand 대시보드                      [새 스캔 시작 →]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────┐  ┌─────────────────────────────┐  │
│  │ AI 가시성 노출도     │  │      최근 스캔 요약          │  │
│  │                     │  │                             │  │
│  │     ┌───────┐      │  │  경쟁사 비교 세트 (3질문)    │  │
│  │     │ 68%  │      │  │  완료: 2026-02-05 14:32     │  │
│  │     │ ████  │      │  │  노출도: 68%                │  │
│  │     └───────┘      │  │                             │  │
│  │                     │  │  [상세 보기 →]               │  │
│  │  ▲ +5% vs 지난주   │  └─────────────────────────────┘  │
│  └─────────────────────┘                                    │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📈 질문별 노출도 (최근 스캔 기준)                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  질문                          Gemini    GPT   평균  │   │
│  │ ───────────────────────────────────────────────────│   │
│  │  "MyBrand vs Notion 비교"       72%      68%   70%  │   │
│  │  "MyBrand vs Slack 장단점"      65%      62%   64%  │   │
│  │  "MyBrand 대안 추천"            70%      71%   71%  │   │
│  │                                                     │   │
│  │  [질문별 상세 →]                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  🏆 경쟁사 점유율 (설정된 경쟁사)                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  ████████████████████████████░░░░░ MyBrand   68%   │   │
│  │  ██████████████████░░░░░░░░░░░░░░░ Notion    45%   │   │
│  │  ████████████░░░░░░░░░░░░░░░░░░░░░ Slack     32%   │   │
│  │  ████████░░░░░░░░░░░░░░░░░░░░░░░░░ Asana     25%   │   │
│  │                                                     │   │
│  │  * 설정에서 경쟁사 추가/수정 가능                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📝 개선 권장사항 (경쟁사 대비 낮은 항목)                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  • Notion 대비: 협업 기능 강조 필요                   │   │
│  │    (Notion은 "실시간 협업" 언급 시 자주 추천됨)       │   │
│  │                                                     │   │
│  │  • Slack 대비: 가격 경쟁력 어필 필요                  │   │
│  │    (Slack은 "저렴한 요금제" 질문에서 높은 노출)       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 가시성 추이 (`/analytics`)

**UX 결정: 세트 전체 + 질문별 드릴다운**

```
┌─────────────────────────────────────────────────────────────┐
│  📈 가시성 추이                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  질문 세트: [경쟁사 비교 세트 ▾]                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 100%│                                               │   │
│  │     │                    ╭──●                       │   │
│  │  80%│           ●───────╯                          │   │
│  │     │      ╭───╯                                   │   │
│  │  60%│ ●───╯                                        │   │
│  │     │                                               │   │
│  │  40%│───────────────────────────────────────────── │   │
│  │     │  1/15    1/22    1/29    2/5                 │   │
│  │     │                                               │   │
│  │     │  ━━ 세트 평균  •• Q1  •• Q2  •• Q3           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [✓] 질문별 상세 보기                                        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  일자      세트평균   Q1     Q2     Q3    변화      │   │
│  │ ─────────────────────────────────────────────────── │   │
│  │  2/5       68%      70%    64%    71%    ▲ +5%    │   │
│  │  1/29      63%      65%    60%    65%    ▲ +5%    │   │
│  │  1/22      58%      60%    55%    60%    ▲ +3%    │   │
│  │  1/15      55%      57%    52%    58%    -        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  💡 분석: "MyBrand vs Notion" 질문에서 지속적 상승세          │
│          (+13% over 3주)                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 스캔 히스토리 (`/history`)

```
┌─────────────────────────────────────────────────────────────┐
│  📜 스캔 히스토리                         [필터 ▾] [내보내기] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  필터: [모든 세트 ▾] [모든 상태 ▾] [최근 30일 ▾]             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📅 2026-02-05                                       │   │
│  │                                                     │   │
│  │ ┌─────────────────────────────────────────────────┐│   │
│  │ │ ✅ 경쟁사 비교 세트              14:32    68%   ││   │
│  │ │ 3개 질문 × 100회 = 300 API 콜 | 450 크레딧     ││   │
│  │ │                                                 ││   │
│  │ │ ├─ Q1: MyBrand vs Notion (70%)                 ││   │
│  │ │ ├─ Q2: MyBrand vs Slack (64%)                  ││   │
│  │ │ └─ Q3: MyBrand 대안 (71%)                      ││   │
│  │ │                                                 ││   │
│  │ │                                    [상세 →]    ││   │
│  │ └─────────────────────────────────────────────────┘│   │
│  │                                                     │   │
│  │ ┌─────────────────────────────────────────────────┐│   │
│  │ │ ✅ SaaS 추천 세트                 09:15    72%   ││   │
│  │ │ 5개 질문 × 100회 = 500 API 콜 | 750 크레딧     ││   │
│  │ │                                    [상세 →]    ││   │
│  │ └─────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📅 2026-02-04                                       │   │
│  │ ...                                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  페이지: [< 이전] 1 [2] 3 4 5 [다음 >]                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.4 스캔 상세 보기 (모달/페이지)

```
┌─────────────────────────────────────────────────────────────┐
│  📊 스캔 상세: 경쟁사 비교 세트                         [X]  │
│  2026-02-05 14:32 | 소요: 18분 32초 | 450 크레딧            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  전체 노출도: 68%                                            │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  질문 1: "MyBrand vs Notion 비교해줘"                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Gemini: 36/50 언급 (72%)  ████████████████░░░░   │   │
│  │  GPT:    34/50 언급 (68%)  █████████████░░░░░░░   │   │
│  │  평균: 70%                                          │   │
│  │                                                     │   │
│  │  경쟁사 언급률:                                      │   │
│  │  • Notion: 90% (45/50)                             │   │
│  │  • Slack:  24% (12/50)                             │   │
│  │                                                     │   │
│  │  감성: 긍정 60% | 중립 35% | 부정 5%                  │   │
│  │                                                     │   │
│  │  [응답 샘플 보기 ▾]                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  질문 2: "MyBrand vs Slack 장단점" [펼치기 ▾]               │
│  질문 3: "MyBrand 대안 추천해줘" [펼치기 ▾]                  │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  📝 개선 권장사항                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • Notion 대비: 협업 기능 차별화 필요                  │   │
│  │   (Notion은 "실시간 협업" 언급 시 90%로 추천됨)      │   │
│  │                                                     │   │
│  │ • 가격 메시지 보완 필요                               │   │
│  │   (가격 관련 질문에서 경쟁사 대비 낮은 노출)          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  📎 인용된 소스 (API 응답에 포함된 경우)                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ • https://mybrand.com/features (3회 인용)          │   │
│  │ • https://techcrunch.com/mybrand-review (2회 인용)  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 구현 체크리스트

### Phase 1: 데이터베이스 및 기본 구조 (Week 1)
- [ ] 마이그레이션 파일 작성 (`20260206_add_batch_scan_tables.sql`)
- [ ] Supabase에 마이그레이션 적용
- [ ] TypeScript 타입 정의 (`types/batch-scan.ts`)
- [ ] 기존 타입 파일 업데이트

### Phase 2: 질문 세트 시스템 (Week 2)
- [ ] API: `/api/question-sets` CRUD
- [ ] API: `/api/question-sets/[id]/items` CRUD
- [ ] Hook: `useQuestionSets` SWR 훅
- [ ] UI: 질문 세트 목록 컴포넌트
- [ ] UI: 질문 세트 생성/편집 모달
- [ ] 페이지: `/settings/question-sets`

### Phase 3: 설정 및 스캔 시작 (Week 3)
- [ ] API: `/api/settings/scan` GET/PATCH
- [ ] Hook: `useScanSettings` SWR 훅
- [ ] UI: 설정 페이지 확장 (반복 횟수, 타임아웃)
- [ ] Util: 크레딧 계산 함수
- [ ] Util: 예상 시간 계산 함수
- [ ] UI: 스캔 시작 페이지 개편 (`/scan/new`)

### Phase 4: Inngest 파이프라인 (Week 4)
- [ ] Inngest: `batch-scan/start` 함수
- [ ] Inngest: `batch-scan/resume` 함수
- [ ] Service: `processQuestion` 로직
- [ ] Service: `queryProviderWithTimeout` 함수
- [ ] Service: 진행 상황 업데이트 로직
- [ ] API: `/api/batch-scans` POST (생성)
- [ ] API: `/api/batch-scans/[id]` GET (상세)
- [ ] API: `/api/batch-scans/[id]/pause` POST
- [ ] API: `/api/batch-scans/[id]/resume` POST

### Phase 5: 예외 처리 및 재개 (Week 5)
- [ ] Service: 에러 분류 함수
- [ ] Service: 에러 핸들링 로직
- [ ] Service: 재시도 로직 (exponential backoff)
- [ ] UI: 일시정지 상태 표시
- [ ] UI: 재개 버튼 및 로직
- [ ] 알림: 크레딧 부족 알림
- [ ] 알림: 네트워크 에러 알림

### Phase 6: 대시보드 개선 (Week 6)
- [ ] UI: 메인 대시보드 개편
- [ ] UI: 질문별 노출도 테이블
- [ ] UI: 경쟁사 점유율 (설정된 경쟁사만)
- [ ] UI: 가시성 추이 차트
- [ ] UI: 스캔 히스토리 페이지
- [ ] UI: 스캔 상세 모달/페이지
- [ ] Logic: 개선 권장사항 생성

### Phase 7: 테스트 및 최적화 (Week 7)
- [ ] Unit Tests: 크레딧 계산
- [ ] Unit Tests: 에러 핸들링
- [ ] Integration Tests: 배치 스캔 API
- [ ] E2E Tests: 스캔 플로우
- [ ] 성능 최적화: 진행 상황 폴링
- [ ] 성능 최적화: 대량 데이터 페이징
- [ ] 문서화: API 문서 업데이트

---

## 📌 즉시 시작 가능한 작업

1. **마이그레이션 파일 작성** - 섹션 3의 SQL 사용
2. **TypeScript 타입 정의** - `types/batch-scan.ts`
3. **크레딧 계산 유틸** - `lib/credits.ts`
4. **질문 세트 API** - 기본 CRUD

---

*마지막 업데이트: 2026-02-06*
*작성자: Clawdbot*
